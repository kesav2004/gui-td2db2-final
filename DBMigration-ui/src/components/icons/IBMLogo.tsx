const IBMLogo = ({ className = "" }: { className?: string }) => {
  return (
    <img
      src="/images/item_horizontal_with_1852881195.jpeg"
      alt="IBM Logo"
      className={className}
      aria-label="IBM"
    />
  );
};

export default IBMLogo;
